<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.side.wiki.mapper.DebateBoardMapper">
	
	<select id="getDebateDetail" resultType="com.side.wiki.vo.DebateReplyVO" parameterType="string">
		select doc_title as docTitle, email, content, regdate 
		from debatereply 
		where doc_title = #{docTitle}
		order by reply_no
	</select>

	<insert id="insertReply" parameterType="com.side.wiki.vo.DebateReplyVO">
		insert into debatereply(doc_title, reply_no, email, content, regdate) 
		values(#{docTitle}, debatereply_seq.nextval, #{email}, #{content}, sysdate)
	</insert>
</mapper>